package com.dabenxiang.mimi.model.enums

enum class ChatAdapterViewType {
    DATE_TITLE,

    SENDER_TEXT,
    SENDER_IMAGE,
    SENDER_BINARY,

    RECEIVER_TEXT,
    RECEIVER_IMAGE,
    RECEIVER_BINARY
}